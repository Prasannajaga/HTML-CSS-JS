const d : HTMLDivElement =  document.createElement('div');
const datas : HTMLElement = document.documentElement;
const query : Element | null = document.querySelector('');
const queryAll : NodeListOf<Element>= document.querySelectorAll('');
const getById = document.getElementById('');
